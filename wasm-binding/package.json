{
  "name": "@mchs/wasm-binding",
  "private": true,
  "version": "0.0.0",
  "description": "TypeScript adapter shell for future wasm-pack output; no calculator logic.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.6"
  }
}
