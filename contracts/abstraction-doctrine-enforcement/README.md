# Abstraction doctrine enforcement contract

This contract is metadata-only. It records allowed architecture boundaries
between formula, parameter, classifier, fixture, binding, documentation, app,
and release surfaces.

The contract deliberately rejects direct implementation dependencies and
invented crosswalk assumptions. It does not embed proprietary formulae,
classification tables, mapping rows, or production payloads.
