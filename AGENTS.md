Coding Standards
================

- Use [Ruff](https://docs.astral.sh/ruff/) for linting. Run `ruff check .` before committing.
- Run the test suite with `pytest`.

Pending Work
------------
Refer to `ROADMAP.md` and `TODO.md` for details on features that are still being implemented. In particular see the roadmap section "SAS feature parity for the 2025 calculators".

